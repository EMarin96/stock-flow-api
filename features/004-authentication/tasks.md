# 004 · Authentication and user roles — Tasks

- [x] Domain: create `Role` enum (`Admin`/`Operator`/`ReadOnly`) — `src/Domain/Users/Role.cs`
- [x] Domain: create `User : AuditableEntity` with `Username`/`NormalizedUsername`/`PasswordHash`/`Role`, `Create(...)` guard clauses, `UpdateRole(...)`, `ResetPassword(...)` — `src/Domain/Users/User.cs`
- [x] Domain: add `Guid? createdBy`/`updatedBy` parameters to `Product.Create`/`Update`, `Location.Create`/`Update`, `StockMovement.Create`
- [x] Application: add `ErrorType.Unauthorized` + `Error.Unauthorized(...)` factory — `Common/Results/Error.cs`
- [x] Application: `Common/Security/` — `IPasswordHasher`, `IJwtTokenGenerator`, `ICurrentUserService`
- [x] Application: `Users/Shared/` — `IUserWriteRepository`, `IUserReadRepository`, `UserDto`, `AuthenticationRecord`, `UserErrors`, mapping extensions
- [x] Application: implement `CreateUser` slice (Command + Validator + Handler — username uniqueness pre-check + DB-race backstop, password hashing)
- [x] Application: implement `GetUsers` slice (Query + Validator + Handler — paginated, filter by username/role)
- [x] Application: implement `GetUserById` slice
- [x] Application: implement `UpdateUser` slice (Command + Validator + Handler — role change, optional password reset)
- [x] Application: implement `DeactivateUser` slice (Command + Validator + Handler — soft delete, rejects self-deactivation)
- [x] Application: implement `Auth/Login` slice (Command + Validator + Handler — generic invalid-credentials error for unknown/wrong-password/deactivated)
- [x] Application: retrofit `ICurrentUserService` into `CreateProductHandler`/`UpdateProductHandler`/`CreateLocationHandler`/`UpdateLocationHandler`/`CreateStockMovementHandler` to populate `CreatedBy`/`UpdatedBy`
- [x] Infrastructure: `PasswordHasher` (PBKDF2, constant-time verify) — `Security/PasswordHasher.cs`
- [x] Infrastructure: `JwtOptions` (Options Pattern) + `JwtTokenGenerator` — `Security/JwtOptions.cs`, `Security/JwtTokenGenerator.cs`
- [x] Infrastructure: configure EF Core mapping for `User` (unique index on `NormalizedUsername`, soft-delete query filter) — `UserConfiguration.cs`
- [x] Infrastructure: implement `UserWriteRepository` (EF) and `UserReadRepository` (Dapper, including `GetForAuthenticationAsync`)
- [x] Infrastructure: register new services/repositories/options in `DependencyInjection.cs`
- [x] Infrastructure: create and apply EF Core migration `AddUsersTable`
- [x] Api: `Security/CurrentUserService.cs` (implements `ICurrentUserService` via `IHttpContextAccessor`)
- [x] Api: implement `POST /api/auth/login` (anonymous) — `src/Api/Endpoints/AuthEndpoints.cs`
- [x] Api: implement Users CRUD (`POST`/`GET`/`GET` list/`PUT`/`DELETE`, all Admin-only) — `src/Api/Endpoints/UserEndpoints.cs`
- [x] Api: wire JWT authentication (`AddAuthentication().AddJwtBearer(...)`) and authorization (`FallbackPolicy`, `WriteAccess`/`AdminOnly` policies) in `Program.cs`
- [x] Api: add the startup admin-bootstrap block (seed from `Seed:AdminUsername`/`AdminPassword`, fail-fast if missing while `Users` is empty)
- [x] Api: retrofit `.RequireAuthorization("WriteAccess")`/`"AdminOnly"` onto existing Products/Locations/StockMovements POST/PUT/DELETE routes
- [x] Api: add Swagger JWT bearer support (`AddSecurityDefinition`/`AddSecurityRequirement`) and annotations for the new endpoints
- [x] Configuration: add `Jwt`/`Seed` sections (empty placeholders) to `appsettings.Development.json`
- [x] Tests: unit tests for `User.Create`/`UpdateRole`/`ResetPassword` guard clauses
- [x] Tests: unit tests for `PasswordHasher` (round-trip, wrong-password rejection) and `JwtTokenGenerator` (claim contents)
- [x] Tests: unit tests for each Users/Auth handler and validator, including `CannotDeactivateSelf` and the generic invalid-credentials path
- [x] Tests: extend `ApiFactory`/`ApiFactoryFixture` with a way to obtain a bearer token per role
- [x] Tests: update every existing Products/Locations/StockMovements integration test to use an authorized `HttpClient`
- [x] Tests: integration tests for Users/Auth endpoints, plus explicit 401 (no/invalid token) and 403 (wrong role) cases per endpoint group
- [x] Validate against the acceptance criteria in `spec.md`
- [x] Move the feature to "Done" in `constitution/roadmap.md` (left for verification, per SDD process)
